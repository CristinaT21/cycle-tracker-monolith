"""
Admin interface configuration for the Visualizations module.

This module doesn't have its own models, so no admin configuration is needed.
"""

# No admin registration needed as this module doesn't have its own models
