"""
Models for the Visualizations module.

This module primarily uses data from other modules
and doesn't need its own models.
"""

# This module uses data aggregation services rather than storing its own data
# It queries data from cycles, analytics, and users modules
